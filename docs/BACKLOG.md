# 🗂 Backlog — Time Tracking Desktop App

Бэклог задач для приложения учёта времени (Log Owl).

---

## 🔹 UX / UI улучшения

- [ ] Реализовать горячие клавиши для таймера (например, Space → start/pause)
- [ ] Добавить трей-иконку, чтобы приложение могло жить свернутым в фоне
- [ ] Добавить пояснения пользователю о округлении времени до минут (для отчетов)
- [ ] **Уведомления об успешном обновлении**: показывать пользователю toast/snackbar при успешных действиях (название изменено, комментарий обновлён/добавлен, интервал добавлен, интервал отредактирован/удалён и т.д.)
- [ ] Сделать возможность редактирования и добавления комментариев для закрытых задач
- [ ] Позволить повторное открытие completed задачи и редактирование
- [ ] Возможность показа истории комментариев (много комментариев, версия истории)

---

## 🔹 Функциональные расширения

- [ ] Idle auto pause / авто-остановка таймера при бездействии
- [ ] Поддержка будущих интервалов вручную (manual entry в будущем)
- [ ] Проверка длинных интервалов и предупреждение (> 12 часов)
- [ ] Расширение ручных интервалов: multi-comments, теги для задач
- [ ] Автоматическое детектирование пересечений / аномалий в интервалах

---

## 🔹 База данных / сохранность

- [ ] Кнопка ручного бэкапа базы данных
- [ ] Возможность экспорта отчетов/данных в CSV
- [ ] Улучшения миграций для будущих версий (например, добавление новых полей без слома старых данных)

---

## 🔹 Gamification / юморные фичи (опционально)

- [ ] Минималистичные анимации “сборщика минут” (LogSquirrel / TimeOtter и т.д.)
- [ ] Уведомления “ты собираешь время, молодец!” при долгой работе над задачей

---

## 🔹 View / рефакторинг

- [ ] **TaskListPage**: убрать дублирование — один layout, условный рендер только нижней части (CompletedToggle + CompletedTasksSection)
- [ ] **useTaskPage**: разбить на несколько хуков (useTaskData, useTaskSession, useIntervals и т.д.) для упрощения поддержки и тестов
- [ ] **IntervalsSection / SessionsSection**: передавать только нужные поля и колбэки вместо всего `state` (убрать god object)
- [ ] **DeleteTaskConfirmDialog**: при loading показывать спиннер на кнопке «Удалить»
- [ ] **Layout**: при роутинге по именованным путям вынести проверку `isTaskDetailPage` в константу/хелпер
- [ ] **RecoveryModal**: контент передавать пропсом, если будет с бэка/конфига

---

## 🔹 Electron / архитектура

### Высокий приоритет

- [x] **Обработка ошибок в IPC**: глобальная обёртка для всех IPC-хендлеров — try/catch, логирование в main, возврат структурированной ошибки в рендерер (`{ ok: false, error: string }`) вместо «сырого» throw. _Сделано: handleIpc + IpcResponse/unwrapIpcResponse._

- [ ] **Валидация аргументов IPC**: проверка на границе (например, Zod) — не пускать невалидные данные в репозитории, возвращать 400-подобный ответ. Защита от багов в UI и случайных вызовов с неверными данными.

### Средний приоритет

- [ ] **Единый источник правды для IPC**: убрать дублирование объявления каналов и API между main (ipcMain.handle), preload (ALLOWED_CHANNELS + expose) и view (Window.electron в vite-env.d.ts). Вариант: общий модуль с объектом каналов (IPC_CHANNELS), по нему строить preload и использовать в main; тип ElectronApi вынести в shared и подключать во view.

- [ ] **Каскадное удаление в БД**: сейчас каскад только в коде (`taskRepository.remove`). Либо добавить `FOREIGN KEY ... ON DELETE CASCADE` в миграциях и упростить `remove`, либо явно задокументировать, что каскады только в коде. Улучшит целостность данных и упростит код.

### Низкий приоритет

- [ ] **Размещение логики персиста окна (windowState)**: разобраться, где лучше держать load/save состояния главного окна (bounds, fullscreen). Сейчас используется из `app/window.ts` (создание окна, подписки) и `app/lifecycle.ts` (save при before-quit) — два потребителя в app, поэтому неочевидно класть всё в `app/window/`. Варианты: оставить отдельной папкой (windowState, state/window и т.п.), вынести в app с подпапкой, или иначе; зафиксировать решение и при необходимости переименовать/переместить файлы.

- [ ] **Два key-value репозитория**: `appStateRepository` и `settingsRepository` почти идентичны — вынести общую логику (getKey/setKey по таблице) или оставить два репозитория, но убрать дублирование SQL. Уменьшит дублирование, но не критично.

- [ ] **Тесты в electron**: расширить покрытие тестами репозиториев и runner миграций (in-memory SQLite). Уже есть базовая структура (`electron/test/dbHelper.ts`, примеры тестов для `taskRepository` и `runner`). Добавить тесты для остальных репозиториев (`timeEntryRepository`, `taskSessionRepository`, `settingsRepository`, `appStateRepository`).

---

## 🔹 Notes / технический долг

- [ ] Многооконность задач (несколько открытых страниц задач одновременно)
